<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:controls="clr-namespace:Habits.Controls"
             x:Class="Habits.MainPage">

    <ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            Spacing="25">

            <DatePicker x:Name="DatePicker" />
            
            <controls:HabitButton HabitName="Gym" 
                                  Date="{Binding Source={x:Reference DatePicker}, Path=Date }" />
            <controls:HabitButton HabitName="Work at home" 
                                  Date="{Binding Source={x:Reference DatePicker}, Path=Date }" />
            <controls:HabitButton HabitName="Work at office" 
                                  Date="{Binding Source={x:Reference DatePicker}, Path=Date }" />

            <controls:History />

            <Label Text="v0.2" FontSize="10" HorizontalOptions="Center" />
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
